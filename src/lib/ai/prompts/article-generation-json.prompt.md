Respond with **only** a valid JSON object. Do not wrap in markdown code fences.

Required JSON shape:

```json
{
  "title": "string",
  "slug": "string",
  "summary": "string",
  "content": "string (Markdown)",
  "seoTitle": "string (max 60 chars)",
  "metaDescription": "string (max 160 chars)",
  "tags": ["string", "string"]
}
```
